import random


def esPrimo(n):
    if n < 2:
        return False
    if n == 2:
        return True
    if n % 2 == 0:
        return False

    i = 3
    while i * i < n:
        if n % i == 0:
            return False
        i += 2
    return True


def coins():
    escudos = 0
    casos = 100000

    for _ in range(casos):
        number = random.randint(0, 9)
        if number & 1:
            escudos += 1

    print(f"Escudos {escudos / casos}, Coronas {(casos - escudos) / casos}\n")


def probabilities():
    a = 0
    b = 0
    c = 0
    casos = 1000000

    for _ in range(casos):
        number = random.randint(0, 9)

        if not number:
            a += 1
        elif esPrimo(number):
            b += 1
        else:
            c += 1

    print(f"a: {a / casos}, b = {b / casos}, c = {c / casos}\n")


def ignore():
    dados = [0 for _ in range(6)]
    casos = 1000000
    i = 0
    while i < casos:
        number = random.randint(0, 9)
        if number < 6:
            dados[number] += 1
        else:
            continue

        i += 1

    for i in dados:
        print(i / casos, end=" ")
    print("\n")


def ignore2():
    caras = 6
    dados = [0 for _ in range(caras)]
    casos = 1000000

    tamano_bloque = 100 // caras

    limite_rechazo = tamano_bloque * caras

    i = 0
    while i < casos:
        number = random.randint(0, 99)

        if number < limite_rechazo:
            dados[number // tamano_bloque] += 1
        else:
            continue
        i += 1

    for i in dados:
        print(i / casos, end=" ")
    print("\n")


print("A:")
print(
    "Si el valor pseudo aleatorio es par le asignamos corona y los impares el escudo, como tenemos misma cantidad de pares e impares se puede conseguir una distribucion uniformede probabilidad\n")

print("Prueba")
coins()

print("B:")
print(
    "La probabilidad de salir 0, la probabilidad de salir un numero primo y la probabilidad de tener un numero compuesto con el 1\n")

print("Prueba")
probabilities()

print("C:")
print("Utilizar los primeros primeros numeros menores a 6 e ignorar los demas\n")

print("Prueba")
ignore()

print("D:")

"""
¿Se pueden simular dados con todos los poliedros regulares?
SI. Se puede aplicar la misma logica de rechazo para los 5 solidos platonicos (4, 6, 8, 12 y 20 caras)
usando el generador de 0 a 99. Solo se divide 100 entre n caras:

- 4 Caras: 100/4 = 25 exacto. No se desperdicia nada. (Rango 0-99)
- 6 Caras: 100/6 = 16 enteros. Usamos 96 numeros. Se rechazan 4.
- 8 Caras: 100/8 = 12 enteros. Usamos 96 numeros (12*8). Se rechazan 4.
- 12 Caras: 100/12 = 8 enteros. Usamos 96 numeros (8*12). Se rechazan 4.
- 20 Caras: 100/20 = 5 exacto. No se desperdicia nada. (Rango 0-99)
"""

print(
    "Ignorar como lo haciamos anteriormente, pero calculando los rangos dinamicamente: 100//6 = 16, y 16*6 = 96. Esto demuestra que SI se pueden simular todos los poliedros regulares (4, 6, 8, 12, 20) usando esta misma logica de division y rechazo.\n")

print("Prueba")
ignore2()